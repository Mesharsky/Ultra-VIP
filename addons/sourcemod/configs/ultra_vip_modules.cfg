"UltraVIP - Module Configuration"
{
    // Add each service from the ultra_vip_main.cfg into here like the below example.
    // Then configure each module setting for each service as needed.
    //
    // Settings can be ignored if they are optional, in which case a default value
    // set by the module plugin will be used.
    // If a required setting is forgotten for any service, you will get an error
    // in the SourceMod error logs.

    "Service Name"                  // MUST have a section for each service in ultra_vip_main.cfg
    {
        "Section"                   // Sections like this are just for organising things better.
        {
            "module_setting" "123"  // A configured module setting.
        }
    }
}
